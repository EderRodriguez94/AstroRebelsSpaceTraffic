# Release evidence

Store dated Android candidate logs and device evidence here during final
validation. Do not store signing keys, passwords or private device data.

The first release does not include iOS evidence. Empty folders and pending
checklist rows are intentional until APK/device validation is authorized.
