# Astro Rebels Traffic traceability

Plane contains the operational backlog and task state. The files below contain the product and technical source of truth for this repository.

File integrity and history are managed through Git commits. Validate repository changes with `git diff --check`, review the staged diff, and record the resulting commit. No file hashes are maintained here.

The source documents are the copies installed in this repository. No files are copied from the unrelated `AstroRebels` action-game repository.
